# Nixpacks configuration for React Frontend on Railway

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
# Express server with proper cache headers for SPA
cmd = "node server.js"
