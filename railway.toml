# Railway configuration for monorepo
# Used by: backend, reverb, scheduler services

# This file is at root level but each service has its own config
# Backend service should set rootDirectory = "backend" in Railway Dashboard
# Frontend service should set rootDirectory = "frontend" in Railway Dashboard

[build]
watchPatterns = ["/backend/**"]
