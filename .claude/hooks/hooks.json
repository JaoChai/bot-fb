{
  "description": "BotFacebook autonomous system hooks",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "[SESSION:INIT] BotFacebook (Laravel 12 + React 19). Auto-load from mem: 1) Rules: [RULE:*][UNIVERSAL], [RULE:*][TECHNOLOGY:Laravel|React], [RULE:*][PROJECT:BotFacebook] - Priority: PROJECT>TECH>UNIVERSAL. 2) Tools: [MCP:REGISTRY], [SKILL:REGISTRY] for tools-manager. 3) Budget: Apply token limits per task size. Cache all for session."
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PROJECT_ROOT}/.claude/scripts/sync-lessons.js\" 2>/dev/null || true; node \"${CLAUDE_PROJECT_ROOT}/.claude/scripts/sync-profile.js\" 2>/dev/null || true",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
