{
  "description": "BotFacebook auto-sync lessons (ไม่เปลือง tokens)",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PROJECT_ROOT}/.claude/scripts/sync-lessons.js\" 2>/dev/null || true",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
