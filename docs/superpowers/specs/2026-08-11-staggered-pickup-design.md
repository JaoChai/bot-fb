# ทยอยเบิกของ → เสนอให้ซื้อทีละตัว (ราคาเท่ากัน) — Design

**วันที่:** 2026-08-11
**เป้าหมาย:** ลูกค้าที่จะจ่ายก้อนใหญ่แล้วขอทยอยรับของทีหลัง → บอทเสนอให้จ่ายเท่าที่จะรับตอนนี้ก่อน (ราคาต่อหน่วยเท่ากัน สั่งเพิ่มทีหลังได้) และห้ามรับปากว่าจะ "เก็บของไว้ให้เบิกทีหลัง" เพราะระบบทำไม่ได้จริง

## บริบท (ตรวจจากของจริงแล้ว)

**เคสจริง — conv #1451 (LINE bot 26) วันที่ 2026-08-11 เวลา 19:47-19:49 น.**

| เวลา | ใคร | ข้อความ |
|------|-----|---------|
| 19:47 | ลูกค้า | `เบิกก่อนแค่ 1 เฟสนะครับ` |
| 19:48 | ลูกค้า | `ผมจ่ายไป 3 แต่จะขอเบิกแค่ 1 ก่อนนะครับ` |
| 19:48 | บอท | `เข้าใจครับพี่ ... ส่วนที่เหลือเก็บไว้เบิกภายหลังได้ครับ` ← **สัญญาลอย** |
| 19:49 | ลูกค้า | `ถ้ายังไม่เบิกยังไม่นับเวลาการเคลมใช่ไหมครับ` |
| 19:49 | บอท | ตอบเลี่ยง โยนลิงก์ Terms |

ตะกร้าตอนนั้น: Nolimit Personal (เติมเงิน) 1,100 × 3 + Page 199 × 3 = **3,897 บาท**
สถานะปัจจุบัน: conversation เป็น `handover` (assigned_user_id=14) — คนรับช่วงคุยแทนบอทแล้ว

**ทำไมบอทตอบแบบนั้น**

1. `flows` id=24 (v26, 29,248 chars) **ไม่มีคำว่า "เบิก" / "ทยอย" / "ทีละ" เลยแม้แต่คำเดียว** (ค้นแล้ว `position()` = 0 ทุกคำ) → LLM ด้นสดขึ้นมาเอง
2. ระบบ **ไม่มีกลไกฝากของ**: `AccountDeliveryService.php:121` วน `for ($u = 0; $u < $qty; $u++)` จอง/ส่งของครบ qty ทันทีที่ยืนยันเงินเข้า ไม่มีตาราง credit/ยอดค้างเบิกในฐานข้อมูล
3. เรื่องเคลม: prompt กับ KB มีแค่ "ดูรายละเอียดที่ลิงก์ Canva" ไม่มีระยะเวลาหรือจุดเริ่มนับ (ค้น `24 ชั่วโมง` = ไม่พบ) → บอทตอบอะไรเกินนี้ = แต่งเอง

**ความเสี่ยงถ้าไม่แก้ (3 ชั้น)**

| ชั้น | ผลที่ตามมา |
|------|-----------|
| A. สัญญาลอย | รับปากเก็บของไว้ ทั้งที่ระบบไม่บันทึกไว้ที่ไหนเลย = หนี้ที่ไม่มีใครเห็น (ซ้ำรอยเคส conv #46 ที่ยังค้าง 19 ตัว) |
| B. ของเสื่อม | บัญชีถูกตัดสต็อกวันนี้ แต่ลูกค้าเอาไปใช้อีกหลายสัปดาห์ → ตายก่อนใช้แล้วเถียงเรื่องเคลม (ลูกค้าถามเรื่องนี้เองเป๊ะๆ) |
| C. เงินกองของค้าง | ร้านถือเงิน 3,897 แต่ค้างส่งของ 2 ชุด ไม่มีหน้าจอไหนแสดง |

## การตัดสินใจ (ยืนยันจากเจ้าของแล้ว 2026-08-11)

1. **ไม่รับฝากของเด็ดขาด** — เงินเข้า = ส่งของครบทันที ไม่มีข้อยกเว้น
2. **บอทห้ามพูดเรื่องเวลาเคลม/ประกันว่าเริ่มนับตอนไหน** — ทางร้านไม่มีนโยบายชัด ดูเป็นเคสๆ ไป (พูด = สัญญาลอยอีกดอก)
3. **เสนอ "ซื้อทีละตัว" เฉพาะตอนลูกค้าส่งสัญญาณทยอยเบิกเท่านั้น** — ไม่เสนอตอนสั่งจำนวนมากปกติ (จะกดยอดขายตัวเองโดยไม่จำเป็น)
4. **แก้ prompt ใน DB อย่างเดียว ไม่แตะโค้ด** — ไม่ต้องมีตาราง credit ไม่ต้องมี PR backend

## การแก้ไข — 2 จุดใน flow 24

### จุดที่ 1: เพิ่มบล็อกใหม่ `<staggered_pickup>` ต่อจาก `</multi_order>`

วางไว้ก่อน `<context_intelligence>` (ติดกับ `<multi_order>` เพราะเป็นเรื่องนโยบายออเดอร์/จำนวนเหมือนกัน)

```
<staggered_pickup>
TRIGGER: ลูกค้าจะจ่ายจำนวนหนึ่ง แต่ขอรับของน้อยกว่านั้นตอนนี้ แล้วเก็บส่วนที่เหลือไว้รับทีหลัง
คำสัญญาณ: "เบิกก่อน X", "เบิกทีละ", "เอาก่อน X", "รับก่อน X", "ที่เหลือค่อย...", "ค่อยเอา/ค่อยรับ", "เก็บไว้ก่อน", "ยังไม่เอาตอนนี้", "จ่าย X แต่เอา Y ก่อน"
⛔ ไม่ใช่ trigger: "ทยอยส่งก็ได้" / "ตัวไหนส่งก่อนได้ส่งมาเลย" = ลูกค้ายอมให้ร้านทยอยส่ง (เร่งของ) ไม่ใช่ขอฝากของ → ตอบปกติ ห้ามเสนอแยกออเดอร์

⛔ กฎเหล็ก: ร้านไม่มีระบบฝากของ — เงินเข้า = ระบบตัดสต็อกและส่งของครบตามจำนวนทันที
ห้ามพูดเด็ดขาด: "เก็บไว้เบิกทีหลัง", "ฝากไว้ก่อน", "ค้างไว้ได้", "ทยอยเบิกได้"

A. ยังไม่จ่าย (ยังไม่ส่งสลิป) → เสนอแยกออเดอร์ทันที (1 ข้อความ):
"อ๋อ ได้ครับพี่ 👍 แต่บอกไว้ก่อนนะครับ ของเราซื้อกี่ตัวราคาก็เท่ากันครับ ตัวละ [ราคา] พี่ไม่ต้องจ่ายก้อนใหญ่ค้างไว้เลยครับ
แนะนำจ่ายเท่าที่จะรับตอนนี้ก่อนครับ — รอบนี้ [จำนวนที่จะรับ] ตัว [ยอด] บาท พอจะเอาเพิ่มเมื่อไหร่ ทักมาสั่งได้เลยครับ ส่งให้ใน 5-10 นาทีเหมือนเดิม
เอาแบบนี้ไหมครับ? หรือจะรับครบ [จำนวนเต็ม] ตัวเลยก็ได้ครับ"
→ ตอบรับ ("เอา 1 ก่อน"/"ทีละตัว") = แก้ตะกร้าเป็นจำนวนที่จะรับจริง (Page ปรับตามสัดส่วน: รับ 1 ชุด = Page 1 ตัว) แล้วไป Step 2 ปกติ
→ เลือกรับครบ = ขายจำนวนเต็มตามปกติ ห้ามทวงซ้ำ
→ ยืนกรานจะจ่ายเต็มแต่ขอรับทีหลัง = ใช้ B

B. ยืนกราน / จ่ายมาแล้ว → ปิดประตู (1 ข้อความ):
"ต้องขออภัยจริงๆ ครับพี่ ทางร้านไม่มีระบบฝากของไว้เบิกทีหลังนะครับ พอเงินเข้าปุ๊บ ระบบจะตัดสต็อกและส่งของครบตามจำนวนทันทีเลยครับ
ถ้าพี่ยังไม่อยากรับครบตอนนี้ แนะนำจ่ายเฉพาะจำนวนที่จะใช้ก่อนครับ ราคาเท่าเดิมทุกตัว สั่งเพิ่มทีหลังได้ตลอดครับ"
(จ่ายมาแล้ว = ส่งของครบตามยอดที่จ่ายเสมอ ห้ามหน่วงของไว้)

C. ถามเรื่องเวลาเคลม/ประกัน ("ยังไม่เบิกยังไม่นับเวลาเคลมใช่ไหม") → ⛔ ห้ามยืนยันวันเริ่มนับเด็ดขาด
"เรื่องเงื่อนไขรับประกัน ขอให้ดูรายละเอียดที่ https://mhhacoursecontent.my.canva.site/ads-vance นะครับ ถ้าอยากได้คำตอบชัดๆ เดี๋ยวผมให้ทีมงานยืนยันให้อีกทีครับ"

D. STOCK GUARD: ห้ามการันตี "สั่งเพิ่มได้ตลอด" ถ้า STOCK STATUS เหลือน้อยกว่าจำนวนที่ลูกค้าจะทยอยซื้อ → ใช้ "ตอนนี้เหลือ [X] ตัวครับ" แทน
</staggered_pickup>
```

### จุดที่ 2: `<safety_checklist>` ข้อ 4 — ให้ checklist บังคับเข้าบล็อกใหม่

เดิม:
```
 4. MULTI-ORDER? → ออเดอร์ยังไม่จ่าย + เพิ่มของ = รวมใบเดียว / ยอดสลิปตรงออเดอร์ไหน ห้ามสับสน
```

ใหม่:
```
 4. MULTI-ORDER? → ออเดอร์ยังไม่จ่าย + เพิ่มของ = รวมใบเดียว / ยอดสลิปตรงออเดอร์ไหน ห้ามสับสน / ขอทยอยเบิก = ไม่มีระบบฝากของ → <staggered_pickup>
```

**ผลต่อขนาด prompt:** +~1,600 chars (29,248 → ~30,850) ยังต่ำกว่า v25 เดิม (42k) มาก

## สิ่งที่ไม่ทำ (ตัดออกโดยตั้งใจ)

- ❌ ตาราง credit / ยอดค้างเบิก + การ์ดเบิกของ + หน้าจอยอดค้าง → เจ้าของเลือก "ไม่รับฝากของ" แล้ว งานโค้ดทั้งก้อนจึงไม่จำเป็น
- ❌ เสนอ "ซื้อทีละตัว" ทุกครั้งที่สั่ง ≥ 2-3 ตัว → กดยอดขายตัวเองโดยลูกค้าไม่ได้ร้องขอ
- ❌ ให้บอทตอบว่าเคลมเริ่มนับตั้งแต่วันส่งของ → ไม่มีนโยบายจริงรองรับ

## Verification

**ขั้นตอนแก้ (ต้องครบ ห้ามข้าม):**
1. `INSERT INTO flow_audit_logs` action `backup_before_staggered_pickup` เก็บ `system_prompt` เดิมทั้งก้อน (ต่อจาก id=22)
2. `UPDATE flows SET system_prompt = ... WHERE id = 24`
3. เคลียร์ cache prompt บน prod (`railway ssh` → `php artisan cache:forget` ตาม runbook `reference_railway_ssh`) — **ไม่ทำ = prompt เก่ายังวิ่งอยู่**

**เกณฑ์ผ่าน (ทดสอบ prompt ใหม่กับบทสนทนาจริง วิธีเดียวกับเคส BM5):**

| # | อินพุต | ต้องได้ |
|---|--------|---------|
| 1 | conv #1451 ย้อนหลัง (`ผมจ่ายไป 3 แต่จะขอเบิกแค่ 1 ก่อนนะครับ`) | เสนอแยกออเดอร์ตาม script A · **ห้ามมีคำว่า "เก็บไว้เบิกภายหลัง"** |
| 2 | ตอบต่อว่า `เอา 1 ก่อนก็ได้ครับ` | ตะกร้าเหลือ Nolimit 1 + Page 1 = 1,299 บาท (ไม่ใช่ 3,897) |
| 3 | ยืนกราน `ไม่ ผมจะจ่าย 3 เลย แต่เบิกทีหลัง` | ใช้ script B ปิดประตู ไม่รับฝาก |
| 4 | `ถ้ายังไม่เบิกยังไม่นับเวลาการเคลมใช่ไหมครับ` | ตอบตาม C ไม่ยืนยันวันเริ่มนับ |
| 5 | (regression) conv #191 `ตัวไหนส่งก่อนได้ ทยอยส่งมาก็ได้นะครับ` | ตอบเรื่องการส่งปกติ **ห้ามเสนอแยกออเดอร์** |
| 6 | (regression) `เอา Nolimit Personal 3 ตัวครับ` เฉยๆ | ขาย 3 ตัว 3,300 ตามปกติ ห้ามเสนอให้ซื้อทีละตัว |
| 7 | (regression) VIP fast track สั่งซ้ำ | ไม่เปลี่ยนพฤติกรรมเดิม |

## ของค้างที่ต้องจัดการแยก (ไม่อยู่ใน spec นี้)

conv #1451 ตอนนี้อยู่ในมือคน — รอบล่าสุดบอท**ยังไม่เคยส่งสรุปยอด+เลขบัญชี** (ใบล่าสุดในระบบคือของ 2026-08-10 ยอด 1,299) และ**ยังไม่มีออเดอร์ 3,897 ในตาราง `orders`** แต่ลูกค้าส่งรูปมาแล้วเมื่อ 19:53 น. → ต้องเช็คด้วยมือว่าเงินเข้าจริงเท่าไหร่และส่งของครบไหม
